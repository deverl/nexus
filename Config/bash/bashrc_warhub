export VANGUARD_DIRECTORY=${HOME}/develop/vanguard
export JAGUAR_DIRECTORY=${VANGUARD_DIRECTORY}/jaguar
export INFRA_DIRECTORY=${HOME}/develop/infrastructure

addToPath ~/nexus/bin/warhub

alias whlogs='tail -f ${JAGUAR_DIRECTORY}/data/jaguar/logs/bbp.log|hl -F -P'

# alias whup='cd ${JAGUAR_DIRECTORY} ; docker compose up -d debug celery && tail -f data/jaguar/logs/bbp.log|hl -F -P'
# alias whdown='(cd ${JAGUAR_DIRECTORY} ; docker compose down ; docker.prune)'

alias whup=whup.sh
alias whdown=whdown.sh
alias whdownp='whdown.sh -p'

alias whtest='time (cd ${JAGUAR_DIRECTORY} ; date ; docker compose up test ; date ; echo "")'
alias whcov='time (cd ${JAGUAR_DIRECTORY} ; date ; docker compose up coverage ; date ; echo "")'
alias whupdebug='(cd ${JAGUAR_DIRECTORY} ; dco up -d debug celery && dco logs -f debug)'
alias cnjd='(cd ${JAGUAR_DIRECTORY} ; ./connect.sh jaguar-debug)'
alias cnpsql='${JAGUAR_DIRECTORY}/connect.sh jaguar-psql'
alias dpy='docker exec -it jaguar-debug python manage.py'
alias adhoc='docker exec -it jaguar-debug python manage.py adhoc_task_handler'

alias start_dash='nvm use && npm ci && sleep 1 && npm run generate && sleep 1 && ng.start'
alias dashup='nvm use && npm i && sleep 1 && npm run generate && sleep 1 && ng.start'
alias dashupci='nvm use && npm ci && sleep 1 && npm run generate && sleep 1 && ng.start'
alias dashupl='ng.start'

alias deploy.next='open ${VANGUARD_DIRECTORY}/docs/DevOps/deployment/deploying_code_next.md'
alias deploy='open ${VANGUARD_DIRECTORY}/docs/DevOps/deployment/deploying_code.md'

alias authorize_net_test='docker exec -it jaguar-debug python manage.py authorize_net_test'

# alias clauderc='claude --remote-control'

alias linear='linear-cli'

# alias lw='list_webhook_data.sh'

# alias migrate.show='(cd ${JAGUAR_DIRECTORY}/bbp/migrations ; ls -1 | tail -n 20)'

alias rdb=rdb.sh
# alias rdball=rdball.sh

alias psql='psql -h localhost -p 5432 -U core -d '

alias v='cd $VANGUARD_DIRECTORY ; pwd'
alias j='cd $JAGUAR_DIRECTORY ; pwd'
alias i='cd $INFRA_DIRECTORY ; pwd'

function ebt() {
    export BEARER_TOKEN=$1
    echo "BEARER_TOKEN='$BEARER_TOKEN'"
}

export NX_TUI=true

