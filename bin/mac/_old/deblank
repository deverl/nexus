#!/usr/bin/env bash

if [ $# -eq 1 ]
then
    fil=$1

    if [ -f $fil ]
    then
        tfil=/tmp/923879234879

        sed '/^\s*$/d' $fil > $tfil

        mv $tfil $fil
    else
        echo "ERROR: Couldn't find file: $fil"
    fi

else
    echo "ERROR: No file specified"
fi


