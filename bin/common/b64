#!/usr/bin/env bash

if [ $# -gt 0 ]
then
    while [ $# -gt 0 ]
    do
        enc=$(echo -n "$1" | base64 -i -)
        echo "$1  =  $enc"
        shift
    done
else
    echo "Nothing to encode!"
fi

