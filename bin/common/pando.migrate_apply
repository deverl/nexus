#!/usr/bin/env bash

if [ ! -f ./script/generate-sql ]
then
    echo "ERROR: Can't find generate-sql. Are you in the right directory?"
    exit 1
fi

./script/generate-sql

pando migrate diff --target=dev

pando migrate apply --target=dev
