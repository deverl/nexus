#!/usr/bin/env bash

fileList=temp_tags_file.list

find . -type f \( -iname '*.c' -o -iname '*.cpp' -o -iname '*.h' -o -iname '*.hxx' -o -iname '*.hpp' -o -iname '*.hh' -o -iname '*.cc' \) -print > $fileList

ctags -L $fileList

rm -f $fileList

