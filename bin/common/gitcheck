#!/usr/bin/env bash

for D in *
do
    if [ -d "$D" ]
    then
        cd "$D"
        if [ -d ".git" ]
        then
            echo "$D"
            git status
        fi
        cd ..
    fi
done

